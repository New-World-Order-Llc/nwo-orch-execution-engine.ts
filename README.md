# nwo-orch-execution-engine.ts
Executes deterministic workflow steps from immutable execution packets. Validates execution context, runs contract‑defined step logic, and produces reproducible execution results. Prevents nondeterministic branching, external side‑effects, or direct state mutation, ensuring full Beast System 3.0 determinism.
